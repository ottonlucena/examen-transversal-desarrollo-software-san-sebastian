meta {
  name: 40 Eliminar álbum (204)
  type: http
  seq: 3
}

delete {
  url: {{baseUrl}}/api/albumes/{{albumId}}
  body: none
  auth: none
}

headers {
  X-Usuario: diego
}

tests {
  test("204 sin contenido", function() {
    expect(res.getStatus()).to.equal(204);
  });
}
