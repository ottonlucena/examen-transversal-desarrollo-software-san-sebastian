meta {
  name: 14 Nombre duplicado (409)
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/api/albumes
  body: json
  auth: none
}

body:json {
  {
    "nombre": "QATAR 2022",
    "fechaLanzamiento": "2022-08-24",
    "tipoLaminas": "ADHESIVA",
    "totalLaminas": 20
  }
}

tests {
  test("409 nombre ya existe (sin distinguir mayúsculas)", function() {
    expect(res.getStatus()).to.equal(409);
  });
}
