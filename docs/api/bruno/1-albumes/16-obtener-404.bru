meta {
  name: 16 Álbum inexistente (404)
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/api/albumes/999999
  body: none
  auth: none
}

tests {
  test("404 en formato ProblemDetail", function() {
    expect(res.getStatus()).to.equal(404);
    expect(res.body.title).to.equal("Recurso no encontrado");
  });
}
