meta {
  name: 37 Quitar foto (204)
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/api/laminas/{{lamina2Id}}/foto
  body: none
  auth: none
}

headers {
  X-Usuario: equipo
}

tests {
  test("204 sin contenido", function() {
    expect(res.getStatus()).to.equal(204);
  });
}
