meta {
  name: 34 Ver foto (200)
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/laminas/{{lamina2Id}}/foto
  body: none
  auth: none
}

tests {
  test("200 con la imagen PNG", function() {
    expect(res.getStatus()).to.equal(200);
    expect(res.getHeader("content-type")).to.equal("image/png");
  });
}
