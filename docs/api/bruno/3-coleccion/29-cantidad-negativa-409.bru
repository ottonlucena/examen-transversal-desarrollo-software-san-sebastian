meta {
  name: 29 Cantidad negativa (409)
  type: http
  seq: 7
}

patch {
  url: {{baseUrl}}/api/laminas/{{lamina4Id}}/cantidad
  body: json
  auth: none
}

body:json {
  {
    "delta": -1
  }
}

tests {
  test("409 la lámina 4 no tiene copias", function() {
    expect(res.getStatus()).to.equal(409);
    expect(res.body.detail).to.contain("negativa");
  });
}
