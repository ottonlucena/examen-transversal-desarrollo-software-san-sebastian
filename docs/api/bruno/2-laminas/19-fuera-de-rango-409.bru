meta {
  name: 19 Número fuera de rango (409)
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/albumes/{{albumId}}/laminas
  body: json
  auth: none
}

body:json {
  {
    "numero": 21,
    "nombre": "Fuera de rango",
    "tipo": "NORMAL"
  }
}

tests {
  test("409 el número excede el total del álbum", function() {
    expect(res.getStatus()).to.equal(409);
    expect(res.body.detail).to.contain("excede");
  });
}
