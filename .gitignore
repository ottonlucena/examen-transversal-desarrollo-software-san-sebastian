# Variables locales (se versiona .env.example)
.env

# Material del curso: solo referencia local, no se publica
# (enunciado y rubrica de la institucion; la transcripcion contiene nombres de companeros)
/*.pdf
/text-clase

# Fotos subidas en desarrollo
uploads/

# Archivo de 6 MB para la prueba 413 (request 36 de la coleccion Postman); se genera localmente
docs/api/postman/archivos/grande.png

# Informe: el .docx es intermedio (se entrega solo el PDF); build/ guarda la primera pasada del indice
docs/informe/*.docx
docs/informe/build/

# IDE / SO
.idea/
*.iml
.vscode/
.DS_Store
